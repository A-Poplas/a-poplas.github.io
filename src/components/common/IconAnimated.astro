---
export interface Props{
    name: string;
}
const {
  name = "set-1",
} = Astro.props;
---
<!--<svg>
   <title></title>
     <symbol/>
    <use ></use>
  </svg> -->
  {(name === "set-1") && (
    <h1>Test 1</h1>
  )}
    {(name === "set-2") && (
    <h1>Test 2</h1>
    )}
  